import React from 'react'

const AuthLayouth = ({children}) => {
    return (
        <div>
            authenticacion layout
            {children}
        </div>
    )
}

export default AuthLayouth
